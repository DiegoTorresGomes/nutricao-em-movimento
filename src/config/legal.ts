/**
 * Versioning of the legal documents and consent, used to build an auditable
 * LGPD consent history. Bump the relevant version whenever a policy's content
 * changes so future consents are recorded against the new version.
 */

// Matches CONSENT_VERSION in src/lib/consent.ts (cookie banner schema).
export const CONSENT_VERSION = 1;

// Date-based versions (ISO) of the published legal pages.
export const PRIVACY_POLICY_VERSION = "2026-07-18";
export const COOKIES_POLICY_VERSION = "2026-07-18";
